#include <bits/stdc++.h>

using namespace std;
typedef long long ll;

#define boost() ios::sync_with_stdio(0); cin.tie(0); cout.tie(0);
#define endl '\n';

const int MM = 1e6+5;

int main() {
    boost();
    string s; ll x, n;
    cin >> s >> s >> x;
    cin >> n;
    cout << n << endl;
}